
import dotenv from 'dotenv';
dotenv.config({ path: '.env.local' }); // Ensure .env.local is loaded for genkit dev

// Flows will be imported for their side effects in this file.
// import './flows/property-recommendation-flow'; // Removed as AI recommendations are temporarily disabled
